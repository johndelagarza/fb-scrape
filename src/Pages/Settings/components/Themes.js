import React from 'react'

function ThemeSettings({ hidden }) {
    console.log(hidden)
  return (
    <div className='bg-white'>
        ThemeSettings
    </div>
  )
};

export default ThemeSettings;
